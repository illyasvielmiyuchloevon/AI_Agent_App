{
  "name": "ai-agent-app-electron",
  "version": "0.0.1",
  "main": "main.js",
  "description": "Electron shell for AI Agent App",
  "scripts": {
    "build:preload": "node scripts/build-preload.js",
    "dev:preload": "node scripts/build-preload.js --watch",
    "dev": "concurrently \"npm run dev:preload\" \"npm --prefix ../frontend run dev\" \"npm run start:electron:preload\"",
    "start:electron": "cross-env VITE_DEV_SERVER_URL=http://localhost:4173 ELECTRON_OPEN_DEVTOOLS=1 electron .",
    "start:electron:preload": "npm run build:preload && npm run start:electron",
    "build": "npm run build:preload && npm --prefix ../frontend run build && electron-builder",
    "test": "node --test",
    "lint": "echo \"No lint configured for electron shell\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3",
    "electron": "^31.2.0",
    "electron-builder": "^24.13.3",
    "fast-check": "^4.5.3"
  },
  "build": {
    "appId": "com.aiagent.app",
    "files": [
      "../frontend/dist/**/*",
      "main/**/*",
      "main.js",
      ".bundles/preload.cjs",
      "preload.js",
      "package.json"
    ],
    "directories": {
      "output": "dist"
    },
    "asar": true
  },
  "dependencies": {
    "chokidar": "^3.6.0",
    "minimatch": "^9.0.5",
    "semver": "^7.6.3",
    "simple-git": "^3.30.0",
    "tar": "^7.5.0",
    "yauzl": "^3.2.0"
  }
}
